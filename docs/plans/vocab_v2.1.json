{
  "version": "v2.1-data-driven-activity-pattern",
  "drafted_at": "2026-05-13",
  "status": "generated — Activity and Pattern facets are data-driven (KMeans+LLM), Matter and lesson_type preserved from v2.0",
  "scheme": "faceted classification (Matter / Activity / Pattern) + lesson_type enum",
  "notes": "Activity facet: embed→KMeans(8)→LLM named, data-driven from 696 record activity phrases. Pattern facet: embed→KMeans(6)→LLM named, data-driven from 696 record pattern phrases. Matter facet and lesson_type preserved from vocab_v2.0.",
  "facets": {
    "matter": {
      "description": "What technical subject the record is about. Each record gets 1-2 matter tags.",
      "tags": [
        {
          "name": "llm_agent_runtime",
          "definition": "LLM API calls (DashScope, OpenAI, Anthropic), prompt template management, model output parsing (JSON / structured output), agent tool invocation, token streaming, embedding generation. Covers chat model behavior, tool calling protocols, and any DashScope/OpenAI/Anthropic SDK usage. Boundary: not about LangGraph orchestration (use langgraph_state) — about the model and SDK layer."
        },
        {
          "name": "langgraph_state",
          "definition": "LangGraph StateGraph topology, nodes/edges definitions, state schema (TypedDict / Pydantic), reducers, checkpointers (MemorySaver / SqliteSaver), thread_id management, interrupt/Command HITL. Covers StateGraph quirks, state field merging rules, and graph compilation. Boundary: not about the LLM model itself (use llm_agent_runtime)."
        },
        {
          "name": "persistence_db",
          "definition": "Persistence-layer and database operations: SQLite/relational schema design, migrations (column add/drop/rename), INSERT/SELECT statements, transaction boundaries, DEFAULT handling, index design. Covers KnowledgeStore, sqlite3 module, schema evolution and on-disk storage. Boundary: not about in-memory state (use langgraph_state) or file-based config (use config_env)."
        },
        {
          "name": "filesystem_path",
          "definition": "File and directory operations: path manipulation (pathlib, os.path), file existence/permission checks, symlinks vs hardlinks, glob patterns, directory traversal, atomic writes. Boundary: not about file content semantics like JSON parsing (use json_serialization)."
        },
        {
          "name": "git_vcs",
          "definition": "Git operations: commit, branch, merge (fast-forward / no-ff), rebase, stash, diff, status, gitignore semantics, pull request workflow, conventional commit conventions. Boundary: not about CI/CD pipelines on top of git (use build_deployment)."
        },
        {
          "name": "testing_framework",
          "definition": "Test framework specifics: pytest/unittest test discovery, fixture lifecycle, mocking patterns, assertion APIs, test isolation, integration test setup, coverage tools, FakeEmbeddings / test doubles. Boundary: not about the act of writing tests (use Activity:testing); this is the technical object being used."
        },
        {
          "name": "http_api",
          "definition": "HTTP request/response handling: REST endpoints, status codes (200/401/403/404), middleware execution order, authentication headers, FastAPI/Starlette/Flask routing, request lifecycle. Boundary: not about CLI command interfaces (use cli_argv)."
        },
        {
          "name": "async_concurrency",
          "definition": "Async/await semantics: asyncio event loops, asyncio.to_thread, threading.Thread, locks, race conditions, async-sync bridging, ToolNode sync requirements. Covers thread-safety, concurrent execution, and async wrapper patterns. Boundary: not about LangGraph parallelism (use langgraph_state)."
        },
        {
          "name": "cli_argv",
          "definition": "Command-line interface design: argparse/typer/click setup, flag/subcommand structure, argv parsing, stdin/stdout/stderr handling, exit codes, shell escaping. Boundary: not about scripts as orchestration scaffolding (use Activity:configuring or deploying)."
        },
        {
          "name": "frontend_browser",
          "definition": "Browser automation and rendering: Playwright/CDP page lifecycle, DOM querying, JavaScript execution, cookie/session injection, WAF detection, frontend rendering performance. Boundary: not about HTTP-level concerns (use http_api)."
        },
        {
          "name": "config_env",
          "definition": "Configuration and environment management: environment variables, .env file loading (dotenv), config files (pyproject.toml, settings.py), API keys/secrets propagation, runtime config injection. Boundary: not about deployment automation (use build_deployment)."
        },
        {
          "name": "package_dependency",
          "definition": "Package manager and dependency operations: pip/uv/poetry/npm installation, virtualenv creation, version pinning, lock files (uv.lock), pyproject.toml manipulation, dependency conflict resolution. Boundary: not about runtime imports (use Activity:configuring if about setup)."
        },
        {
          "name": "json_serialization",
          "definition": "Structured data formats: JSON/YAML/TOML parsing and generation, Pydantic models, schema validation, dict-to-JSON conversion edge cases, serialization gotchas (datetime, None, NaN). Boundary: not about LLM output as JSON (use llm_agent_runtime)."
        },
        {
          "name": "logging_observability",
          "definition": "Log emission and visibility: structured logging (JSONL events), log levels, stack trace propagation, metrics emission, telemetry, alerting infrastructure, observability gaps. Boundary: not about debugging activity (use Activity:debugging) — this is the logging mechanism itself."
        },
        {
          "name": "datetime_handling",
          "definition": "Date/time values: timezone handling (UTC, ISO 8601), date arithmetic, epoch conversion, date string parsing, time-partitioned data structures. Covers datetime module and timezone-aware comparisons. Boundary: focused on temporal correctness, not on scheduling (use Activity:deploying for cron)."
        },
        {
          "name": "build_deployment",
          "definition": "CI/CD pipelines, GitHub Actions workflows, Docker builds, Kubernetes deployments, release automation, deployment script orchestration. Boundary: not about local package management (use package_dependency)."
        }
      ]
    },
    "activity": {
      "description": "What kind of engineering work was happening when the lesson surfaced. Each record gets exactly 1 activity tag.",
      "tags": [
        {
          "name": "debugging",
          "definition": "Debugging is identifying and resolving defects that manifest as incorrect, unexpected, or unintended behavior in running software systems."
        },
        {
          "name": "refactoring",
          "definition": "Refactoring is restructuring existing code or data logic to improve internal quality without changing external behavior."
        },
        {
          "name": "orchestrating",
          "definition": "Orchestrating is coordinating the execution, state flow, and interaction of distributed or sequential components within a workflow or agent system."
        },
        {
          "name": "retrieving",
          "definition": "Retrieving is acquiring relevant information from heterogeneous sources to support reasoning or decision-making Concrete signals: hybrid search strategy, retrieval recall evaluation, citation fidelity tuning, status-aware summarization, structural drift detection in workflows."
        },
        {
          "name": "tool_looping",
          "definition": "tool_looping involves validating, implementing, and debugging the end-to-end LLM tool invocation loop—including prompt design, fallbacks, retries, input validation, response parsing, and synchronization between prompt logic and runtime dispatch—ensuring robust, machine-parsable, and deterministic agent interactions."
        },
        {
          "name": "commit_messaging",
          "definition": "Commit_messaging is authoring structured, semantically meaningful commit messages that reflect the intent and scope of code changes."
        },
        {
          "name": "configuring",
          "definition": "Configuring is setting up and managing the parameters, inputs, and environmental conditions that control how a CLI-style interface or tool behaves at runtime."
        },
        {
          "name": "testing",
          "definition": "Testing is designing, executing, and maintaining executable checks that validate behavioral correctness and stability of software under controlled conditions."
        }
      ]
    },
    "pattern": {
      "description": "The recurring abstract lesson the record exemplifies. Each record gets 0-2 pattern tags (pattern facet is optional — some lessons are tool-discovery without a general pattern).",
      "tags": [
        {
          "name": "shared_mutable_state",
          "definition": "Shared mutable state is the use of state that can be modified by multiple actors or contexts without built-in coordination, leading to non-determinism, inconsistency, or isolation failures."
        },
        {
          "name": "explicit_contract",
          "definition": "Explicit contract is the principle of defining and enforcing interface boundaries through unambiguous, machine- or human-verifiable specifications rather than relying on implicit assumptions, runtime behavior, or implementation artifacts."
        },
        {
          "name": "boundary_validation",
          "definition": "Boundary validation is the principle that semantic correctness and data integrity must be enforced at the earliest possible boundary—typically input parsing—rather than deferred to downstream logic or structural assumptions."
        },
        {
          "name": "implicit_assumption_failure",
          "definition": "Implicit assumption failure is when a system fails silently or opaquely because it relies on unstated, unvalidated assumptions about state, context, type, or control flow—especially across boundaries."
        },
        {
          "name": "implicit_dependency",
          "definition": "Implicit dependency is a design flaw where components rely on hidden, undeclared, or convention-based assumptions about shared state, environment, or structure—leading to fragility under composition, testing, or deployment."
        },
        {
          "name": "output_validation_first",
          "definition": "Output validation first is prioritizing structural and semantic correctness of results over input sanitization or process fidelity, ensuring validity emerges from observable outcomes rather than assumed correctness of intermediate steps."
        }
      ]
    }
  },
  "lesson_type": {
    "description": "The polarity/type of the lesson. Each record gets exactly 1 lesson_type. This is an enum field, not a tag facet.",
    "values": [
      {
        "name": "anti_pattern",
        "definition": "What NOT to do. A mistake, fragile approach, or naive solution that causes failures. The record's tone is 'don't / avoid / never / breaks'. Examples: 'Mocking the DB hides migration failures', 'Hand-crafting plist XML is error-prone'."
      },
      {
        "name": "discovery",
        "definition": "A surprising or non-obvious behavior of a tool, framework, or system. The lesson is the discovery itself, documenting how something actually works (often contrary to expectation). Examples: 'StateGraph silently drops unknown keys', 'fast-forward merge creates no commit'."
      },
      {
        "name": "best_practice",
        "definition": "A recommended approach or correct way to handle X. The record's tone is 'should / prefer / use Y'. Examples: 'asyncio.to_thread for blocking IO', 'plistlib over string templating'."
      }
    ]
  }
}