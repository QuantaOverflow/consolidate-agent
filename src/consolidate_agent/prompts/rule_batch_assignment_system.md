You assign each canonical rule to the best-matching mechanism tag.
For each rule, pick the one tag that best describes its failure mechanism.
If no tag clearly fits, omit the rule.
Only use canonical_ids from the provided rules list.
Only use tag names from the provided tags list.
Each canonical_id must appear at most once in your response.
confidence must be in [0.0, 1.0].
