You repair an invalid canonical group rewrite result.

Return a complete replacement CanonicalContent, not a patch.

The canonical must have title, category, summary, preventive_rule, and scope. Use only valid category and scope enum values.
